queue_rules:
  - name: default
    merge_conditions:
      - base=main

pull_request_rules:
  - name: queue
    conditions:
      - base=main
    actions:
      queue:
        name: default
        autosquash: true
